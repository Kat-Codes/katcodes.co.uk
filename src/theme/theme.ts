import { DefaultTheme } from 'styled-components';

const myTheme: DefaultTheme = {
  borderRadius: '5px',

  colors: {
    foreground: '#333',
    background: 'white',
    footer: '#ddd',
  },
};

export { myTheme };